module.exports = {
        get_all_apeals: 'SELECT * FROM requests',
        get_apeals_by_status: 'SELECT * FROM requests WHERE status = ?',
        get_apeals_by_date: 'SELECT * FROM requests WHERE createdAt = ?',
        get_apeals_by_between_date: 'SELECT * FROM requests WHERE DATE(createdAt) BETWEEN ? AND ?',
        take_apeal: 'UPDATE requests SET status = "В работе" WHERE id = ?',
        complete_apeal: 'UPDATE requests SET status = "Завершено" WHERE id = ?',
        cancel_apeal: 'UPDATE requests SET status = "Отменено" WHERE id = ?',
        cancel_all_in_progress: 'UPDATE requests SET status = "Отменено" WHERE status = "В работе"',
      };
